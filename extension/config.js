// Extension configuration — swap values for production build
const CONFIG = {
  // GoAds API base URL (no trailing slash)
  API_URL: "http://localhost:3000/api/extension",
  // GoAds website URL for sign-in redirect
  SITE_URL: "http://localhost:3000",
};
